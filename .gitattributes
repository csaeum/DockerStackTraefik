# .gitattributes - Export Configuration

# Exclude development and CI/CD files from archives (git archive / GitHub releases)
/.github export-ignore
/.idea export-ignore
/tests export-ignore
.gitignore export-ignore
.gitattributes export-ignore
.editorconfig export-ignore

# Keep all documentation (important for users)
# CHANGELOG.md, DEPLOYMENT.md, ENV-CONFIGURATION.md, LABELS-CHECKLIST.md
# are all user-facing documentation and should be included in releases

# Keep .env.example (important template for users)

# Keep logs directory structure but exclude content
/logs/*.log export-ignore
/logs/*.gz export-ignore

# Exclude volumes (certificates, etc.)
/volumes export-ignore
